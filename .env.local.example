# Resend API key for the sandbox-access form. Sign up at https://resend.com.
# When this is unset (e.g. fresh deploy), the API route returns 503 and the
# client falls back to the mailto: behaviour from the original site.
RESEND_API_KEY=
